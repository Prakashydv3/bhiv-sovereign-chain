# BHIV Sovereign Chain - Handover Documentation\n\n## How to Run\n\n### Prerequisites\n- Go 1.21+\n- Git\n\n### Quick Start\n```bash\n# Clone and setup\ngit clone <repo-url>\ncd bhiv-sovereign-chain\ngo mod tidy\n\n# Run complete demo\ngo run integration/e2e_demo.go\n\n# Start anchor API server\ngo run -c \"import anchor; anchor.StartAnchorAPI('8080')\"\n```\n\n### Expected Output\n```\n=== BHIV Sovereign Chain Day 2 Demo ===\n\n1. Executing real TTS request...\nEnvelope created: intent_1703123456789\nDecision: tts_generated (confidence: 0.85)\n\n2. Signing execution envelope...\nEnvelope hash: a1b2c3d4e5f6...\nSignature: 9f8e7d6c5b4a...\n\n3. Anchoring execution on L1...\nAnchor ID: 1a2b3c4d5e6f...\n\n4. Verifying anchor...\nAnchor verified: true\n\n5. Testing deterministic replay...\nReplay successful: intent_1703123456789\nOriginal hash: a1b2c3d4e5f6...\nReplay hash:   a1b2c3d4e5f6...\nDeterministic: true\n\n6. Execution trace:\n  14:30:15: input (success: true)\n  14:30:15: envelope (success: true)\n  14:30:15: anchor (success: true)\n\n=== Demo Complete ===\nTTS Response: 26 bytes audio, 2.6s duration\n```\n\n## How to Anchor\n\n### Programmatic Anchoring\n```go\nimport (\n    \"bhiv-sovereign-chain/l1-core/anchor\"\n    \"bhiv-sovereign-chain/shared/envelope\"\n)\n\n// 1. Create execution envelope\nenv := envelope.NewEnvelope(\"your-system\", \"your-agent\")\nenv.InputHash = sha256.Sum256(inputData)\n// ... execute your logic ...\nenv.OutputHash = sha256.Sum256(outputData)\nenv.Decision = \"your-decision\"\nenv.ConfidenceScore = 0.95\n\n// 2. Validate envelope\nif err := env.Validate(); err != nil {\n    log.Fatal(err)\n}\n\n// 3. Anchor on L1\nenvelopeHash := env.Hash()\nvar parentHash [32]byte // Use previous anchor or zero for genesis\nanchorID, err := anchor.Submit(envelopeHash, parentHash, time.Now().Unix())\n```\n\n### HTTP API Anchoring\n```bash\n# Start API server\ngo run -c \"import anchor; anchor.StartAnchorAPI('8080')\"\n\n# Submit anchor request\ncurl -X POST http://localhost:8080/anchor_execution \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"envelope\": {\n      \"intent_id\": \"intent_123\",\n      \"system_id\": \"your-system\",\n      \"agent_id\": \"your-agent\",\n      \"decision\": \"approved\",\n      \"confidence_score\": 0.95,\n      \"timestamp\": 1703123456\n    },\n    \"signature\": \"base64-signature\",\n    \"signer_id\": \"agent-001\"\n  }'\n```\n\n## How to Replay\n\n### Single Execution Replay\n```go\nimport \"bhiv-sovereign-chain/shared/replay\"\n\n// Initialize replay engine\nreplayEngine := replay.NewReplayEngine()\n\n// Replay execution with original input\nreplayEnv, err := replayEngine.ReplayExecution(originalEnvelope, originalInput)\nif err != nil {\n    // Replay failed - determinism violation\n    log.Fatal(err)\n}\n\n// Verify determinism\nif originalEnvelope.Hash() != replayEnv.Hash() {\n    log.Fatal(\"Determinism violation detected\")\n}\n```\n\n### Chain Replay\n```go\n// Replay sequence of executions\nexecutions := []replay.ReplayInput{\n    {OriginalEnvelope: env1, Input: input1},\n    {OriginalEnvelope: env2, Input: input2},\n}\n\nfinalHash, err := replayEngine.ReplayChain(executions)\nif err != nil {\n    log.Fatal(\"Chain replay failed\")\n}\n```\n\n## How to Integrate New Systems\n\n### Step 1: Implement System Processor\n```go\n// Create new file: integration/yoursystem/processor.go\npackage yoursystem\n\nimport \"bhiv-sovereign-chain/shared/envelope\"\n\ntype YourProcessor struct {\n    SystemID string\n    AgentID  string\n}\n\nfunc NewYourProcessor() *YourProcessor {\n    return &YourProcessor{\n        SystemID: \"your-system-id\",\n        AgentID:  \"your-agent-id\",\n    }\n}\n\nfunc (p *YourProcessor) ProcessRequest(request YourRequest) (*envelope.ExecutionEnvelope, *YourResponse, error) {\n    // 1. Create envelope\n    env := envelope.NewEnvelope(p.SystemID, p.AgentID)\n    \n    // 2. Compute input hash\n    inputData := serializeInput(request)\n    env.InputHash = sha256.Sum256(inputData)\n    \n    // 3. Execute your logic\n    response := executeYourLogic(request)\n    \n    // 4. Compute output hash\n    outputData := serializeOutput(response)\n    env.OutputHash = sha256.Sum256(outputData)\n    \n    // 5. Set execution details\n    env.Decision = \"your-decision\"\n    env.ConfidenceScore = calculateConfidence(request)\n    env.PolicyID = \"your-policy-id\"\n    env.DatasetIDs = []string{\"your-dataset-ids\"}\n    env.EnvironmentHash = sha256.Sum256([]byte(\"your-env-version\"))\n    \n    // 6. Validate\n    if err := env.Validate(); err != nil {\n        return nil, nil, err\n    }\n    \n    return env, response, nil\n}\n```\n\n### Step 2: Add Replay Support\n```go\n// Update shared/replay/replay.go\nfunc (r *ReplayEngine) ReplayExecution(originalEnvelope *envelope.ExecutionEnvelope, input interface{}) (*envelope.ExecutionEnvelope, error) {\n    switch originalEnvelope.SystemID {\n    case \"gurukul-tts\":\n        return r.replayTTS(originalEnvelope, input)\n    case \"your-system-id\":  // Add your system\n        return r.replayYourSystem(originalEnvelope, input)\n    default:\n        return nil, fmt.Errorf(\"unsupported system: %s\", originalEnvelope.SystemID)\n    }\n}\n\nfunc (r *ReplayEngine) replayYourSystem(originalEnv *envelope.ExecutionEnvelope, input interface{}) (*envelope.ExecutionEnvelope, error) {\n    yourRequest, ok := input.(yoursystem.YourRequest)\n    if !ok {\n        return nil, fmt.Errorf(\"invalid input type\")\n    }\n    \n    // Re-execute\n    processor := yoursystem.NewYourProcessor()\n    replayEnv, _, err := processor.ProcessRequest(yourRequest)\n    if err != nil {\n        return nil, err\n    }\n    \n    // Verify determinism\n    return replayEnv, r.verifyDeterminism(originalEnv, replayEnv)\n}\n```\n\n### Step 3: Update Demo\n```go\n// Add to integration/e2e_demo.go\nfunc demonstrateYourSystem() {\n    processor := yoursystem.NewYourProcessor()\n    \n    request := yoursystem.YourRequest{\n        // Your request fields\n    }\n    \n    env, response, err := processor.ProcessRequest(request)\n    if err != nil {\n        log.Fatal(err)\n    }\n    \n    // Anchor and verify as shown in existing demo\n}\n```\n\n## System Architecture\n\n### Data Flow\n```\nInput → System Processor → Execution Envelope → Hash → L1 Anchor\n  ↓           ↓                    ↓            ↓        ↓\nReal        Real Logic         Canonical    Deterministic Immutable\nData        Execution          Format       Hash         Truth\n```\n\n### Key Components\n- **Execution Envelope**: Canonical format for all executions\n- **L1 Anchor**: Immutable truth layer for state roots\n- **Replay Engine**: Deterministic re-execution verification\n- **Tracer**: Complete execution logging\n- **Signer**: Cryptographic signing for authenticity\n\n## Troubleshooting\n\n### Common Issues\n\n**Issue**: \"envelope validation failed: system_id required\"\n**Solution**: Ensure all required envelope fields are populated\n\n**Issue**: \"determinism violation detected\"\n**Solution**: Check for non-deterministic elements (timestamps, random values)\n\n**Issue**: \"anchor already exists\"\n**Solution**: Use different parent hash or ensure unique state roots\n\n### Debug Mode\n```go\n// Enable detailed logging\ntracer := tracing.NewTracer(\"./debug-logs\")\ntracer.LogInput(intentID, input)\ntracer.LogEnvelope(envelope)\ntracer.LogAnchor(intentID, anchorID)\n```\n\n### Verification Commands\n```bash\n# Check logs\ncat logs/trace_*.jsonl | jq .\n\n# Verify determinism\ngo test ./shared/replay -v\n\n# Test anchor system\ngo test ./l1-core/anchor -v\n```\n\n## Production Deployment\n\n### Environment Setup\n```bash\n# Production environment variables\nexport BHIV_LOG_LEVEL=info\nexport BHIV_ANCHOR_API_PORT=8080\nexport BHIV_TRACE_DIR=/var/log/bhiv\n```\n\n### Monitoring\n- Monitor anchor API health: `GET /health`\n- Check trace logs for execution patterns\n- Verify determinism with periodic replay tests\n- Monitor signature verification success rates\n\n### Security\n- Use secure key management for signers\n- Validate all input envelopes\n- Rate limit anchor API endpoints\n- Audit trace logs regularly\n\n**Status**: Ready for production integration with all BHIV systems